{{- $fields := slice 
  "date_from" 
  "date_to" -}}
{{- $fields = partial "admin/func/GetFields.html" (dict "field" . "fields" $fields) -}}

{{- $args := dict 
  "label" (i18n "admin.fields.visits.label" 2)
  "label_singular" (i18n "admin.fields.visits.label" 1)
  "name" "visits"
  "collapsed" false
  "fields" $fields
  -}}

{{ partial "admin/widgets/list.js" $args }}
